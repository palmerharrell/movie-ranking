API_TOKEN=
ALLOWED_ORIGIN=http://localhost:5173
PORT=3001
# Search & Suggest (#243) live TMDb lookups only — every other endpoint
# serves already-enriched data and needs no key. Optional: the two
# suggestion endpoints just answer 503 until this is set.
TMDB_API_KEY=
